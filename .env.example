# Pacific Bending Carbone - Environment Configuration
# Copy to .env and customize for local development
# For production, set these in Dokploy UI

# =============================================================================
# CARBONE STUDIO CONFIGURATION
# =============================================================================

# Enable Carbone Studio for template editing
# SECURITY: Only enable on trusted networks or with authentication
# When enabled: https://pb-templates.erp.observer/studio (production)
#              http://localhost:4000/studio (local)
CARBONE_STUDIO_ENABLED=false

# Optional: Basic authentication for Studio access
# Format: username:password
# Example: CARBONE_STUDIO_AUTH=admin:your-secure-password
# CARBONE_STUDIO_AUTH=

# =============================================================================
# ENTERPRISE LICENSE (Optional)
# =============================================================================
# Required for enterprise features:
#   - Multi-node deployment with shared storage
#   - S3/Azure Blob Storage integration
#   - Advanced PDF features
# CARBONE_EE_LICENSE=your-license-key

# =============================================================================
# ADVANCED CONFIGURATION (Optional)
# =============================================================================
# These rarely need to be changed

# Number of document factory workers (default: auto based on CPU)
# CARBONE_EE_FACTORIES=4

# Bind address (default: 0.0.0.0)
# CARBONE_EE_BIND=0.0.0.0

# Port (default: 4000)
# CARBONE_EE_PORT=4000

# Render timeout in milliseconds (default: 60000)
# CARBONE_EE_TIMEOUT=60000
